Hi Ohata-san, sharing the updated answers based on our current understanding of the PicoDet ONNX:

1. Current output classes
The smoke test was run with 2 classes: basketball and volleyball for 2 epochs. So the current model is a 2-class model, not 1-class or 80-class.


2. What happens after the ONNX part? Is it mainly NMS?
There is no NMS in the ONNX graph. The ONNX output already contains the decoded bounding boxes and class scores.
The post-processing after ONNX includes confidence filtering, class selection, and NMS. We are currently checking the complete post-processing flow and will share the details once confirmed.


3. Is this the correct URL for the PicoDet code?
No. For this implementation, we used LibreYOLO. LibreYOLO's PicoDet port follows the Bo396543018/Picodet_Pytorch implementation.


4. Why did the ONNX output shape change from (1, 3190, 4) to (1, 3190, 6)?
The earlier (1, 3190, 4) output contained only the 4 bounding-box coordinates (x1, y1, x2, y2).
The current (1, 3190, 6) output additionally includes the 2 class scores for basketball and volleyball. Therefore, the 6 values are: x1, y1, x2, y2, basketball_score, volleyball_score.
This change is related to the exported output definition, rather than simply the image-size change.


5. Target speed
The current inference time is around 3.82 ms, while the target is around 2.0–2.5 ms. We will work on model optimization to achieve the target speed.

Hi Ohata-san,

**1. Output classes** – Currently 2 classes, not 1 (ball) or 80. Worth confirming whether that's expected for this checkpoint, or if it should be trimmed to 1 class for the ball-only use case.

**2. What happens after the ONNX part / is it mainly NMS?** – NMS is intentionally not in this graph (confirmed — no NMS op present), per the earlier decision to handle it later due to hardware constraints. The graph does handle DFL decode and box decode internally (raw head outputs → x1,y1,x2,y2), and it also outputs 2 class scores per anchor alongside the box — that's why the output is `[x1,y1,x2,y2,score_class0,score_class1]` (6 values), unrelated to NMS. So what's needed outside the ONNX model, before NMS is added later, is just confidence thresholding on those scores.

**3. GitHub link correct?** – Structurally consistent with a PyTorch PicoDet implementation (matches the LibreYOLO port mentioned), but worth confirming it's the exact repo used, since authorship/version can't be verified from the ONNX file alone.

**4. Shape change (1,3190,4) → (1,3190,6)** – Not caused by the image size change. It's 4 (box coords only) vs 6 (box coords + 2 class scores concatenated). Since the current smoke-test exports are all 6, the earlier `4`-shape export was likely from a version where scores weren't concatenated into the output yet (or class count/head config was different) — tied to where this came from in the port/training, not input resolution.

**5. Target speed 2.0–2.5ms** – No blocker seen from the model graph itself; happy to help profile once NMS and final class count are locked in, since both will affect real end-to-end latency.

